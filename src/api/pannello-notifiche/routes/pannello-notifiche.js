'use strict';

/**
 * pannello-notifiche router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::pannello-notifiche.pannello-notifiche');
